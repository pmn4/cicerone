<% if block.is_a?(BeerNewsletterBlock) %>
	<%= render partial: 'shared/newsletter_block__beer', locals: { block: block } %>
<% else %>
	<%= render partial: 'shared/newsletter_block__content', locals: { block: block } %>
<% end %>